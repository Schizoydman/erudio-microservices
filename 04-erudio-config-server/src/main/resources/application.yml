server:
  port: 8888
spring:
  application:
    name: erudio-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Schizoydman/erudio-config-server
          #username: Schizoydman
          #password: Mzm987031
          default-label: main
          search-paths:
          - 'greeting-service*'